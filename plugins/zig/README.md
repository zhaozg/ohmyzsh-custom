# zig autocomplete plugin

* Adds autocomplete options for all zig commands.
* Add autocomplete for `zig`

## Requirements

In order to make this work, you will need to have the zig tools set up in your path.


